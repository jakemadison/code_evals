__author__ = 'jmadison'


import fileinput


def main():

    for line in fileinput. input(files='./testfile'):
    # for line in fileinput.input():
        line = line.strip()
        if not line:
            continue

        line = int(line)

    start_point = [0]

    # maybe we can define this as it's position is relative to the number of changes that have happened:
    # 0 - 1
    # 01 - 2
    # 0112 - 4
    # 01121220 - 8



if __name__ == '__main__':
    main()












